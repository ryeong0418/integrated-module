CREATE TABLE AE_DB_SQL_STAT (
    DATA_TYPE VARCHAR( 20 ) ,
    SNAP_ID NUMERIC NULL,
    INSTANCE_NUMBER NUMERIC NULL,
    INSTANCE_NAME VARCHAR( 48 ) NULL,
    BEGIN_TIME TIMESTAMP NULL,
    END_TIME TIMESTAMP NULL,
    SQL_ID VARCHAR( 20 ) NULL,
    PARSING_SCHEMA_NAME VARCHAR(128) NULL,
    PLAN_HASH_VALUE NUMERIC NULL ,
    SHARABLE_MEM NUMERIC NULL,
    FORCE_MATCHING_SIGNATURE NUMERIC NULL,
    MODULE VARCHAR( 64 ) NULL,
    ELAPSED_TIME_DELTA NUMERIC NULL,
    CPU_TIME_DELTA NUMERIC NULL,
    IOWAIT_DELTA NUMERIC NULL,
    EXECUTIONS_DELTA NUMERIC NULL,
    DISK_READS_DELTA NUMERIC NULL,
    BUFFER_GETS_DELTA NUMERIC NULL,
    ROWS_PROCESSED_DELTA NUMERIC NULL,
    identifier VARCHAR (50) NULL,
    create_dt timestamp with time zone default current_timestamp,
    create_id varchar(20) default 'system' not null
     )
